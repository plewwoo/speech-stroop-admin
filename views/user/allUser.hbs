<nav aria-label="breadcrumb">
	<ol class="breadcrumb">
		<li class="breadcrumb-item"><a href="/">แดชบอร์ด</a></li>
		<li class="breadcrumb-item active">ผู้ใช้งานทั้งหมด</li>
	</ol>
</nav>
<h1 class="h2 text-center mb-3">ผู้ใช้งานทั้งหมด</h1>
<div class="row">
	<div class="col-12 col-xl-12 mb-4 mb-lg-0">
		<div class="card">
			<div class="card-header d-flex justify-content-between align-items-center">
				<h5 class="m-0">ตารางแสดงผู้ใช้งานทั้งหมด</h5>
				<div>
					<a class="btn btn-outline-secondary btn-sm" href="/export-csv/users">CSV</a>
					<a class="btn btn-outline-secondary btn-sm" href="/export-json/users">JSON</a>
				</div>
			</div>
			<div class="card-body">
				<div class="table-responsive">
					<table id="sortTable" class="table table-hover table-sortable">
						<thead>
							<tr>
								<th scope="col">#</th>
								<th scope="col">ชื่อผู้ใช้</th>
								<th scope="col">ชื่อจริง</th>
								<th scope="col">อีเมล</th>
								<th scope="col">วันที่สมัครสมาชิก</th>
								<th></th>
							</tr>
						</thead>
						<tbody>
							{{#each result.users}}
							<tr>
								<td class="font-weight-bold">{{paginationIndex @index ../result.page}}</td>
								<td>{{username}}</td>
								<td>{{name}} {{surname}}</td>
								<td>{{email}}</td>
								<td>{{datetimeFormat createdAt 'Do MMM YYYY, HH:mm'}} น.</td>
								<td><a href="/user/{{_id}}" class="btn btn-sm btn-primary">รายละเอียด</a></td>
							</tr>
							{{/each}}
						</tbody>
					</table>
				</div>
			</div>
		</div>
		<nav class="mt-3">
			<ul class="pagination justify-content-center">
				{{#each pageList}}
				{{#usersPagination ../result.pagination this}}
				{{/usersPagination}}
				{{/each}}
			</ul>
		</nav>
	</div>
</div>