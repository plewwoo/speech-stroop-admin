<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="/">แดชบอร์ด</a></li>
    <li class="breadcrumb-item active">ประวัติทั้งหมด</li>
  </ol>
</nav>
<h1 class="h2 text-center mb-3">ประวัติทั้งหมด</h1>
<div class="row">
	<div class="col-12 col-xl-12 mb-4 mb-lg-0">
		<div class="card">
			<div class="card-header d-flex justify-content-between align-items-center">
				<h5 class="m-0">ตารางแสดงประวัติ</h5>
				<div>
					<a class="btn btn-outline-secondary btn-sm" href="/export-csv/histories">CSV</a>
					<a class="btn btn-outline-secondary btn-sm" href="/export-json/histories">JSON</a>
				</div>
			</div>
			<div class="card-body">
				<div class="table-responsive">
					<table class="table table-hover table-sortable">
						<thead>
							<tr>
								<th scope="col">#</th>
								<th scope="col">ชื่อผู้ใช้</th>
								<th scope="col">คะแนนรวม</th>
								<th scope="col">คะแนนส่วนที่ 1</th>
								<th scope="col">คะแนนส่วนที่ 2</th>
								<th scope="col">คะแนนส่วนที่ 3</th>
								<th scope="col">วันที่ทำแบบทดสอบ</th>
								<th scope="col"></th>
							</tr>
						</thead>
						<tbody>
							{{#each result.histories}}
							<tr>
								<td class="font-weight-bold">{{paginationIndex @index ../result.page}}</td>
								{{#each user}} 
								<td>{{username}}</td>
								{{/each}}
								<td>{{totalScore}}</td>
								{{#each sections}}
								<td>{{finalScore score.congruent score.incongruent}}</td>
								{{/each}}
								<td>{{datetimeFormat createdAt 'Do MMM YYYY, HH:mm'}} น.</td>
								<td><a href="/history/{{_id}}" class="btn btn-sm btn-primary">รายละเอียด</a></td>
							</tr>
							{{/each}}
						</tbody>
					</table>
				</div>
			</div>
		</div>
		<nav class="mt-3">
			<ul class="pagination justify-content-center">
				{{!-- {{#each pageList}}
				<li class="page-item"><a class="page-link" href="/histories/{{this}}">{{this}}</a></li>
				{{/each}} --}}
				{{#each pageList}}
				{{#ifPagination ../result.pagination this}}
				{{/ifPagination}}
				{{/each}}
			</ul>
		</nav>
	</div>
</div>